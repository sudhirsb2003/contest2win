Hey <%= @user.username %>,

<p>
  To generate your new password please click on the link below or copy &amp; paste it into your browser.<br/>
  <a href="<%= @controller.url_for(:controller => 'account', :action => :new_password, :token => @token) %>"><%= @controller.url_for(:controller => 'account', :action => :new_password, :token => @token) %></a>
</p>

<p>If you did not request to change your password, please ignore this mail.</p>
<br />
- The c2w Team
